This folder contains the first task assigned at my Internship at PITB

Transformers and datafetchers:
The datafetchers folder uses publicAPI(s) to retrieve the data and the Transformers are used to clean the data and store them into the database(ETL pipeline)

SQL Queries:
The queries.sql contains all the queries demonstrating the use of all the window functions mentioned and the statement for each query is also commented along with the solution.